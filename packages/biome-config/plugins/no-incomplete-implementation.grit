engine biome(1.0)
language js(typescript)

or {
	JsThrowStatement(argument=JsNewExpression(callee=JsIdentifierExpression())) as $match where {
		$match <: r"(?i).*not[ -]?implemented.*|.*todo.*|.*coming soon.*",
		register_diagnostic(span=$match, message="Remove the placeholder implementation", severity="error")
	},
	`$promise.catch(() => null)` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`$promise.catch(() => [])` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`$promise.catch(() => ({}))` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`$promise.catch(($error) => null)` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`$promise.catch(($error) => [])` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`$promise.catch(($error) => ({}))` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`try { $...body } catch ($error) { return null; }` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`try { $...body } catch ($error) { return []; }` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	`try { $...body } catch ($error) { return {}; }` as $match where {
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	},
	JsCatchClause(body=JsBlockStatement(statements=[
		$statement
	])) as $match where {
		$statement <: r"^console\.(?:debug|error|info|log|warn)\(.*",
		register_diagnostic(span=$match, message="Do not leave incomplete implementations or silently replace failures", severity="warn")
	}
}
