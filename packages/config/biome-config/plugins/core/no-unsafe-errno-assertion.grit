engine biome(1.0)
language js(typescript)

or {
	`($value as NodeJS.ErrnoException).code` as $match,
	`($value as NodeJS.ErrnoException)["code"]` as $match
} where {
	register_diagnostic(span=$match, message="Validate the error shape before reading errno properties", severity="warn")
}
