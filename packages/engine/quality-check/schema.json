{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"additionalProperties": false,
	"description": "quality-checkの設定 file",
	"properties": {
		"$schema": {
			"type": "string"
		},
		"baseline": {
			"anyOf": [
				{
					"minLength": 1,
					"type": "string"
				},
				{
					"const": false
				}
			]
		},
		"failOnWarnings": {
			"default": false,
			"description": "Treats finding-engine warnings as errors and passes --error-on-warnings to Biome.",
			"type": "boolean"
		},
		"biome": {
			"additionalProperties": false,
			"properties": {
				"enabled": {
					"default": false,
					"description": "Whether quality-check runs this engine.",
					"type": "boolean"
				},
				"args": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array"
				}
			},
			"type": "object"
		},
		"typecheck": {
			"additionalProperties": false,
			"properties": {
				"enabled": {
					"default": false,
					"description": "Whether quality-check runs this engine.",
					"type": "boolean"
				},
				"args": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array"
				},
				"projects": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array"
				}
			},
			"type": "object"
		},
		"knip": {
			"additionalProperties": false,
			"properties": {
				"enabled": {
					"default": false,
					"description": "Whether quality-check runs this engine.",
					"type": "boolean"
				},
				"args": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array"
				}
			},
			"type": "object"
		},
		"code-style-check": {
			"additionalProperties": false,
			"properties": {
				"enabled": {
					"default": false,
					"description": "Whether quality-check runs this engine.",
					"type": "boolean"
				},
				"includes": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array",
					"default": [
						"**"
					],
					"description": "Ordered globs relative to the project root. Negative globs exclude earlier matches; later positive globs can include them again."
				},
				"rules": {
					"additionalProperties": false,
					"properties": {
						"preset": {
							"default": "recommended",
							"description": "Selects the initial rule set before group and rule overrides.",
							"enum": [
								"recommended",
								"all",
								"none"
							],
							"type": "string"
						},
						"spacing": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"blank-line-between-class-members": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"blank-line-between-definitions": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						}
					},
					"type": "object"
				}
			},
			"type": "object"
		},
		"comment-check": {
			"additionalProperties": false,
			"properties": {
				"enabled": {
					"default": false,
					"description": "Whether quality-check runs this engine.",
					"type": "boolean"
				},
				"includes": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array",
					"default": [
						"**"
					],
					"description": "Ordered globs relative to the project root. Negative globs exclude earlier matches; later positive globs can include them again."
				},
				"rules": {
					"additionalProperties": false,
					"properties": {
						"preset": {
							"default": "recommended",
							"description": "Selects the initial rule set before group and rule overrides.",
							"enum": [
								"recommended",
								"all",
								"none"
							],
							"type": "string"
						},
						"suppression": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"broad-suppression": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"undocumented-directive": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"shape": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"cramped-comment": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"separator-comment": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"content": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"placeholder-comment": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"japanese-period": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						}
					},
					"type": "object"
				}
			},
			"type": "object"
		},
		"document-style-check": {
			"additionalProperties": false,
			"properties": {
				"enabled": {
					"default": false,
					"description": "Whether quality-check runs this engine.",
					"type": "boolean"
				},
				"includes": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array",
					"default": [
						"**"
					],
					"description": "Ordered globs relative to the project root. Negative globs exclude earlier matches; later positive globs can include them again."
				},
				"rules": {
					"additionalProperties": false,
					"properties": {
						"preset": {
							"default": "recommended",
							"description": "Selects the initial rule set before group and rule overrides.",
							"enum": [
								"recommended",
								"all",
								"none"
							],
							"type": "string"
						},
						"whitespace": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"consecutive-blank-lines": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"hard-break-html": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"hard-tabs": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"single-trailing-newline": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"trailing-backslash": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"trailing-whitespace": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"typography": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"emphasis-marker": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"full-width-alphanumeric": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"japanese-comma": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"japanese-period": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"structure": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"code-fence-language": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"emphasis-as-heading": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"fence-blank-lines": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"fence-style": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"heading-blank-lines": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"heading-indent": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"heading-level-jump": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"heading-space": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"heading-trailing-punctuation": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"list-blank-lines": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"list-marker-consistency": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"list-marker-space": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"ordered-list-marker": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"setext-heading": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"table-blank-lines": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"thematic-break-style": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"content": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"date-anchored-statement": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"empty-link": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"reversed-link": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						}
					},
					"type": "object"
				}
			},
			"type": "object"
		},
		"tsdoc-check": {
			"additionalProperties": false,
			"properties": {
				"enabled": {
					"default": false,
					"description": "Whether quality-check runs this engine.",
					"type": "boolean"
				},
				"includes": {
					"items": {
						"minLength": 1,
						"type": "string"
					},
					"type": "array",
					"default": [
						"**"
					],
					"description": "Ordered globs relative to the project root. Negative globs exclude earlier matches; later positive globs can include them again."
				},
				"rules": {
					"additionalProperties": false,
					"properties": {
						"preset": {
							"default": "recommended",
							"description": "Selects the initial rule set before group and rule overrides.",
							"enum": [
								"recommended",
								"all",
								"none"
							],
							"type": "string"
						},
						"syntax": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"blank-line-before-tags": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"orphan-doc": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"single-line-doc": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"tsdoc-syntax": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"tsdoc-tag": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"documentation": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"deprecated-without-guidance": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"missing-doc": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"missing-returns": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"contract": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"param-mismatch": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"param-order": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"param-untagged": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"type-param-mismatch": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"type-param-untagged": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						},
						"suppression": {
							"anyOf": [
								{
									"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
									"enum": [
										"off",
										"on",
										"warn",
										"error"
									],
									"type": "string"
								},
								{
									"additionalProperties": false,
									"properties": {
										"suppression": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										},
										"suppression-unused": {
											"description": "off disables the rule, on keeps its default severity, and warn or error overrides the severity.",
											"enum": [
												"off",
												"on",
												"warn",
												"error"
											],
											"type": "string"
										}
									},
									"type": "object"
								}
							]
						}
					},
					"type": "object"
				},
				"docScope": {
					"enum": [
						"exported",
						"documented",
						"all"
					],
					"type": "string"
				},
				"styleScope": {
					"enum": [
						"exported",
						"documented"
					],
					"type": "string"
				}
			},
			"type": "object"
		}
	},
	"title": "quality.json",
	"type": "object"
}
